# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tcc_seguranca?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=@Pequeno3858
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Jackson — garante ISO 8601 para datas (nunca array)
spring.jackson.serialization.write-dates-as-timestamps=false

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Executa os scripts SQL de inicializacao DEPOIS do Hibernate criar/atualizar as tabelas
spring.jpa.defer-datasource-initialization=true

# Inicializacao de SQL — cria as stored procedures no startup
# O separador ";;" permite que o ";" interno do corpo da procedure seja preservado
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:db/procedures.sql
spring.sql.init.separator=;;
# Remova esta linha - Spring Boot detecta automaticamente
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# DevTools — recarrega templates e reinicia a aplicacao ao salvar (somente em dev)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# SPTrans Olho Vivo API — cadastre seu token em https://www.sptrans.com.br/desenvolvedores/
sptrans.token=0169f0d7a37764dfb7627f61e8a28f246ef92212a69c09ecdb23c2edb02cd5c6

# Server
server.port=8080
server.error.whitelabel.enabled=false
